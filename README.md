# C_Cpp_Sql_Sample_2
User Management System built with C/C++, SQL

# 1. Create a build directory
mkdir build && cd build

# 2. Configure with CMake
cmake ..

# 3. Build executable
cmake --build .

# 4. Run application (Ensure db/schema.sql location is relative to runtime location)
./UserManagementSystem
